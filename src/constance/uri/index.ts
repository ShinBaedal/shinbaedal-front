// export const PROXY = window.location.hostname === "localhost" ? "" : "/proxy";

const uri = {
  store: `/store`,
  menuList: `/menu/list`,
  review: `/review`,
  reviewList: `/review/list`,
  photo: `/photo`,
  order: `/order`,
  myOrder: `/order/my`,
};

export default uri;
